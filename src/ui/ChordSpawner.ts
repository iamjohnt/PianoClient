import ChordObservable from "../keyboard_connection/ChordObservable"

export default class ChordSpawner implements ChordObservable {

    onUpdate(chord: Set<number>): void {
        // draw chord to ui, by hooking into phaser somehow
    }


    
}